PUBLIC_STORE_DOMAIN=your-shop-name.myshopify.com
PUBLIC_STOREFRONT_API_TOKEN=your_storefront_api_access_token
SESSION_SECRET=replace_with_random_long_string
DEV_MOCK_PRODUCTS=1
# DEBUG_INSTRUMENTATION=1
# Example environment configuration for RunnersRotation
# Copy to `.env.local` (local dev) and add these in Vercel Project Settings (Preview + Production).
# Do NOT commit real private tokens.

#############################################
# Minimal Environment for RunnersRotation   #
#############################################

# Required (core storefront features)
PUBLIC_STORE_DOMAIN=runnersrotation.myshopify.com
PUBLIC_STOREFRONT_API_TOKEN=shpat_public_storefront_token_here
SESSION_SECRET=replace_with_a_real_generated_secret

# Optional (development conveniences)
# Enables mock product fallback when a handle is missing (development only)
# DEV_MOCK_PRODUCTS=1

# Enables verbose instrumentation (timings, preflight, /__health) – keep off in production
# DEBUG_INSTRUMENTATION=1

#############################################
# Advanced (NOT needed normally)            #
#############################################
# If in the future you adopt multi-storefront or special CSP needs, you might add:
# PUBLIC_STOREFRONT_ID=gid://shopify/Storefront/<id>
# A checkout domain (Hydrogen CSP helper can accept it) but is optional:
# PUBLIC_CHECKOUT_DOMAIN=checkout.shopify.com

# Do NOT add Admin API tokens here. Only the single Storefront API token above is required.
